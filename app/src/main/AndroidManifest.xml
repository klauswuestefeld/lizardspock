<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="felipebueno.lizardspock">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="Lizard Spock"
        android:theme="@android:style/Theme.DeviceDefault.Dialog">

        <meta-data android:name="SneerApp" android:value=""/>

        <activity
            android:name=".LizardSpockActivity"
            android:excludeFromRecents="true"
            android:exported="true">

            <meta-data android:name="sneer:session-type"
                       android:value="lizard-spock" />
        </activity>
    </application>

</manifest>
